frozy:
  name: connector_name
  tier: sandbox
  http_schema: https
  insecure: true
  broker:
    host: custom.frozy.broker
    port: 2200
  registration:
    http_root: http://custom.reg.cloud
    http_path: /reg/v1/register

auto_registration:
  access_token: "A2vHcq3oahZkwporuJXTJkCPJrHbiCuLWhQcHsDnAmfTf"

applications-config:
  applications: 
    - name: "sql-backend.self.user-domain.sergey-afrozy-dio"
      access_token: "ABVdVrRy3ZC5Q8b3X1K3Nsehf7EJ9yWvfLvLQRHXwec1J"
      host: "localhost"
      port: 5444    
    - name: "web-backend"
      access_token: "asdfasdfasdfasdffff"
      host: "localhost"
      port: 5445        
      
  intents: 
    - src_name: "web-backend"
      dst_name: "sql-backend.self.user-domain.sergey-afrozy-dio"
      access_token: "ABVdVrRy3ZC5Q8b3X1K3Nsehf7EJ9yWvfLvLQRHXwec1J"
      port: 5555  

# Config validation

# auto_registration sections must exist with access_token. If there are no access_token
# in configuration then configuration is considered invalid.

# In config there are either applications.provide or applications.consume or both of sections may exists. 
# If there are none of them then configuration will be enriched from environment variables 
# FROZY_APPLICATIONS_PROVIDE/FROZY_APPLICATIONS_CONSUME and if applications is not configured 
# then configuration is considered invalid.
